apiVersion: v1
kind: Service
metadata:
  name: {{ include "anara.fullname" . }}
  labels:
    {{- include "anara.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "anara.selectorLabels" . | nindent 4 }}
