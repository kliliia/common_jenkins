apiVersion: v1
kind: Service
metadata:
  name: {{ include "csamatov-website-deploy.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "csamatov-website-deploy.name" . }}
    helm.sh/chart: {{ include "csamatov-website-deploy.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "csamatov-website-deploy.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
